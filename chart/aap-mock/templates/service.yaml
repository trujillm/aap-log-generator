apiVersion: v1
kind: Service
metadata:
  name: {{ include "aap-mock.fullname" . }}
  labels:
    {{- include "aap-mock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aap-mock.selectorLabels" . | nindent 4 }}

